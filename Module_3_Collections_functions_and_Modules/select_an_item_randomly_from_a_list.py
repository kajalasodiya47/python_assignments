# Import the 'random' module, which provides functions for generating random values
import random
list1 = [1,2,3,4,5]
# Use the 'random.choice' function to select and print a random number from the list1
print(random.choice(list1))  
