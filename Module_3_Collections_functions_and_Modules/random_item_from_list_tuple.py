'''
  use random.choice() method
  The choice() function takes one argument
  Pass your list as an argument, and it will return a random element from it.

'''
# import random module
import random
# list
my_list = [1,9,11,20,14]
# use random.choice() method
print(random.choice(my_list))
