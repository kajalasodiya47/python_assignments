'''
The seed() method is used to initialize the random generator.
The random number generator needs a number to start with (a seed value),
to be able to generate a random number.
'''
# import the random module
import random
random.seed(6)
print(random.random())
